
/**
 * Write a description of class DistanceAndAngle here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class DistanceAndAngle
{
    public static void main(String[] args){
        
    }
    
    public static double distanceNangle(int x1, int y1, int x2, int y2, int x3, int y3){
        double FirstNSeconddistance();
        double SecondNThirddistance();
        double FirstNThirddistance();
        double FirstNSecondAngle();
        double SecondNThirdAngle();
        double FirstNThirdAngle();
        
        FirstNSeconddistance = Math.sqrt(Math.pow(x2-x1,2)+ Math.pow(y2-y1,2));
        SecondNThirddistance = Math.sqrt(Math.pow(x3-x2,2)+ Math.pow(y3-y2,2));
        FirstNThirddistance = Math.sqrt(Math.pow(x3-x1,2)+ Math.pow(y3-y1,2));
        
        FirstNSecondAngle = Math.acos();
        SecondNThirdAngle = 
        FirstNThirdAngle = 
    }
}
